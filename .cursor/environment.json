{
  "name": "newproject1",
  "install": "npm ci",
  "terminals": [
    {
      "name": "api",
      "command": "npm run dev:server",
      "description": "Express guestbook API (TypeScript via tsx watch) on http://localhost:3100"
    },
    {
      "name": "web",
      "command": "npm run dev:web",
      "description": "Vite React dev server on http://localhost:5180 (proxies /api to the API)"
    }
  ],
  "ports": [
    { "name": "web", "port": 5180 },
    { "name": "api", "port": 3100 }
  ]
}
