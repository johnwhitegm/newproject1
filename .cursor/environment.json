{
  "name": "newproject1",
  "install": "npm ci",
  "terminals": [
    {
      "name": "api",
      "command": "npm run dev:server",
      "description": "Express guestbook API (TypeScript via tsx watch) on http://localhost:4280"
    },
    {
      "name": "web",
      "command": "npm run dev:web",
      "description": "Vite React dev server on http://localhost:5280 (proxies /api to the API)"
    }
  ],
  "ports": [
    { "name": "web", "port": 5280 },
    { "name": "api", "port": 4280 }
  ]
}
