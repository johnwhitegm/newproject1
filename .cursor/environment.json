{
  "name": "newproject1",
  "install": "npm ci",
  "terminals": [
    {
      "name": "api",
      "command": "npm run dev:server",
      "description": "Express guestbook API (TypeScript via tsx watch) on http://localhost:3001"
    },
    {
      "name": "web",
      "command": "npm run dev:web",
      "description": "Vite React dev server on http://localhost:5173 (proxies /api to the API)"
    }
  ],
  "ports": [
    { "name": "web", "port": 5173 },
    { "name": "api", "port": 3001 }
  ]
}
